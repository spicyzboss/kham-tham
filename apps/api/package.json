{
  "name": "api",
  "main": "src/main.ts",
  "scripts": {
    "prebuild": "rimraf ../../dist/api",
    "build": "nest build",
    "dev": "NODE_ENV=development dotenv -e ../../.env.development -- nest start --watch",
    "start": "nest start",
    "start:prod": "NODE_ENV=production node ../../dist/api/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^9.1.4",
    "@nestjs/core": "^9.1.4",
    "@nestjs/platform-fastify": "^9.1.4",
    "@prisma/client": "^4.4.0",
    "dotenv-cli": "^6.0.0",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^3.0.2",
    "rxjs": "^7.5.7"
  },
  "devDependencies": {
    "pino-pretty": "^9.1.1"
  }
}
